name: go_event
description: "Progetto Programmazione Mobile"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http:
  provider:
  geolocator:
  permission_handler:
  hive:
  hive_flutter:
  flutter_dotenv:
  flutter_map:
  latlong2:
  intl:
  url_launcher:
  image_picker:
  path_provider:
  flutter_typeahead:
  animate_do:
  mockito:
  network_image_mock:
  hive_test:
  connectivity_plus:
  flutter_launcher_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/images/profile_default.jpg
    - .env

flutter_icons:
  android: true
  image_path: "assets/icon/icona_app.png"

